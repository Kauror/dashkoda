"""Operations tooling that is verified by the test suite.

Most of `ops/` is shell wrapper examples, which the host copies and adapts. This
package holds the parts that carry real logic and therefore need tests.
"""
